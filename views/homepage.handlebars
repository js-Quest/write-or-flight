{{!-- need to show all posts here, list --}}
<div>
  {{#if posts}}
  {{#each posts as |post|}}
  <div>
   <h1>posts go here</h1>
  </div>
  {{/each}}

  {{else}}
  <h2>There are no current posts</h2>

  {{/if}}
</div>